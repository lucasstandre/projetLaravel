name: my first pipeline
on:
  push:
jobs:
  bonjour:
    runs-on: ubuntu-latest
    steps:
      - name: Dire bonjour
        run: echo "Bonjour from Github Actions"
      - name: Afficher la date
        run: date
      - name: Afficher le repertoire courant
        run: pwd
